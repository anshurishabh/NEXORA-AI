# URL of your running backend (see /backend)
# Local development:
VITE_API_BASE_URL=http://localhost:5000

# Production (after deploying the backend to Render), set this in Vercel's
# Project Settings → Environment Variables instead of this file:
# VITE_API_BASE_URL=https://your-backend.onrender.com
